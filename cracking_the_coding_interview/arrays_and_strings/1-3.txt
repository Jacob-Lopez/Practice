Write a method to replace all spaces in a string with %20.
1) Two passes... 
count the number of spaces & allocate a new array
iterate through the array in reverse order and write to the new array from back to front